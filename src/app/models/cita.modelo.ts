//Define la estructura de una cita en la app
export interface Cita {
  id: number;       //Identificador único de cada cita
  frase: string;    //Texto de la cita
  autor: string;    //Autor de la cita
}